# Sample .travis.yml for R projects

language: r
warnings_are_errors: true
dist: trusty
sudo: required

before_install:
  - sudo apt-get postgresql-server-dev

r_github_packages:
  - MartinRoth/visDec
  - SESYNC-ci/postGIStools

r_packages:
  - covr
after_success:
  - Rscript -e 'covr::codecov()'

