# Sample .travis.yml for R projects

language: r
warnings_are_errors: true
sudo: required

r_github_packages:
  - MartinRoth/visDec

r_packages:
  - covr
after_success:
  - Rscript -e 'covr::codecov()'

